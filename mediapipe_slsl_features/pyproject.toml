[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "mediapipe-slsl-features"
version = "1.0.0"
requires-python = ">=3.11,<3.12"
[tool.pytest.ini_options]
testpaths = ["tests"]

